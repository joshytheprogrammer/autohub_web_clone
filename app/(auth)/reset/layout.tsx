export const metadata = {
  title: 'AutoHub - Rest Password',
  description: 'Autohub',
}

export default function ResetPasswordLayout({ children } : { children: React.ReactNode })
{
    return (
      <html lang="en">
        <body
        >
          {children}
        </body>
      </html>
    )
}